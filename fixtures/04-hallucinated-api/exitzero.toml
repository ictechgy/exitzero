version = 1
plugins = ["exitzero_verify", "exitzero_harness"]

[[checks]]
id = "imports"
kind = "python.imports"
paths = ["src/**/*.py"]
options = { roots = ["src"], allow_modules = [] }

[harness]
config_files = []
rules = []
