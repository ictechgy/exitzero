schema_version = 1
description = "bad Claude hooks, bad TOML mcp_servers and an unrecognized TOML all fail"

[expect.check]
exit = 1
rules = ["harness.config", "harness.config", "harness.config", "harness.config", "harness.config", "harness.config", "harness.config"]

[expect.lint]
exit = 1
rules = ["harness.config", "harness.config", "harness.config", "harness.config", "harness.config", "harness.config", "harness.config"]
