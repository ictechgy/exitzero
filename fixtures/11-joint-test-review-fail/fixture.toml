schema_version = 1
description = "test-quality and review-contract findings must be reported jointly"

[expect.check]
exit = 1
rules = ["test-quality", "review-contract"]

[expect.lint]
exit = 0
rules = []
